#include <stdlib.h>

int main ()
{
#if 0
  /* TODO: */
  updateMAllTwo_child (ma,
		       s,
		       mb,
		       t,
		       raOld,
		       rbOld,
		       eleIdx,
		       qpStart,
		       qpEnd,
		       qpidx,
		       vecP,
		       vecQ,
		       vecS,
		       vecT);
#endif
  exit (0);
}
