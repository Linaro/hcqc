#include <cstdio>
#include <cstdlib>
#include <cmath>
#include "header.hxx"

extern void runCRawLoops_PIC_1D (Real_ptr vx, Real_ptr xx, Real_ptr xi, Real_ptr ex, Real_ptr ex1, Real_ptr dex, Real_ptr dex1, Real_ptr rh, Real_ptr rx, const Real_type flx, Index_type* ix, Index_type* ir, Index_type* grd);

int main ()
{
#if 0
  void runCRawLoops_PIC_1D (vx, xx, xi, ex, ex1, dex, dex1, rh, rx, flx, ix, ir, grd);
#endif
  exit (0);
}
