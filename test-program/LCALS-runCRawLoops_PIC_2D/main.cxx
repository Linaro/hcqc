#include <cstdio>
#include <cstdlib>
#include <cmath>
#include "header.hxx"

extern void runCRawLoops_PIC_2D (Index_type len, Real_ptr* p, Real_ptr* b, Real_ptr* c, Real_ptr y, Real_ptr z, Index_type* e, Index_type* f, Real_ptr* h);

int main ()
{
#if 0
  runCRawLoops_PIC_2D (len, p, b, c, y, z, e, f, h);
#endif
  exit (0);
}
